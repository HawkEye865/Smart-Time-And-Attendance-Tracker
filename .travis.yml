matrix:
  include:
    - language: node_js
      node_js:
        - 12.18.2
      script:
        - npm install
        - npm install newman
        - npm install nodemon
        - cd STAT-server
        - nodemon
        - newman run AutomatedDemo3.postman_collection.json