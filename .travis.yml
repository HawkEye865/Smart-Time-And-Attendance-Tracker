matrix:
  include:
  - language: node_js
    node_js:
    - 12.18.2
    script:
    - cd STAT
    - npm install
    - npm rebuild
    - ng test
    - echo "Finished test"
  branches:
    only:
      - master
      - /^(?i:app)-.*$/