matrix:
  include:
  - language: node_js
    node_js:
    - 12.18.2
    script:
    - cd STAT
    - npm install
    - ng test --watch=false --browsers=ChromeHeadless
  branches:
    only:
      - master
      - /^(?i:app)-.*$/